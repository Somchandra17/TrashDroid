# Presidio PII detection — regex + checksum validators (credit card Luhn, SSN format, etc.)
# Enable with: python main.py --presidio
# Pinned to the tested versions for reproducible installs.
rich==14.3.3
presidio-analyzer==2.2.359
# Presidio's spaCy backend needs a model. After install, run:
#   python -m spacy download en_core_web_lg
